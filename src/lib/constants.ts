// App-wide constants. Keep narrow: contact info, public URLs, brand strings —
// values that may be referenced from multiple screens and need a single source
// of truth. Behavior config (feature flags, thresholds) belongs elsewhere.

export const SUPPORT_EMAIL = 'support@cookwithmesa.com';
