{
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    "strict": true
  },
  "//": "Deno-runtime files are excluded from tsc — they use https: imports and the Deno global, and are type-checked by `deno test` instead.",
  "exclude": ["supabase/functions", "src/**/__tests__"]
}
